# Production Environment Configuration  
# Copy this to .env when testing against production environment
# WARNING: Use with caution on production systems

# Server URLs - Production Environment
FRONTEND_URL=https://board-app.com
BACKEND_URL=https://api.board-app.com

# Server Ports (Standard HTTPS)
FRONTEND_PORT=443
BACKEND_PORT=443

# Automation Settings - Conservative settings for production
TIMEOUT=60s
BROWSER_TYPE=chromium
HEADLESS=true

# Debug Settings
DEBUG=false